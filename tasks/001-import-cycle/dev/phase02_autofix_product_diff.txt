diff --git a/fastapi-application/alembic/env.py b/fastapi-application/alembic/env.py
index 474557e..4eef834 100644
--- a/fastapi-application/alembic/env.py
+++ b/fastapi-application/alembic/env.py
@@ -22,7 +22,9 @@ if config.config_file_name is not None:
 
 # ===== ВСТАВИТЬ настоящий settings.db.url и target_metadata ===== #
 from db_core import Base
+from db_core.model_registry import load_model_registry
 
+load_model_registry()
 target_metadata = Base.metadata
 
 # other values from the config, defined by the needs of env.py,
diff --git a/fastapi-application/db_core/__init__.py b/fastapi-application/db_core/__init__.py
index dbfbf18..c1a229d 100644
--- a/fastapi-application/db_core/__init__.py
+++ b/fastapi-application/db_core/__init__.py
@@ -1,40 +1,5 @@
-__all__ = (
-    "Base",
-    "BlogPost",
-    "BlogUser",
-    "Order",
-    "OrderProductAssociation",
-    "Product",
-    "User",
-)
-
-from ex_order_product.model_order_product import (
-    Order,
-    OrderProductAssociation,
-    Product,
-)
-
-from md_articles.models import (
-    BlogPost,
-    BlogUser,
-)
+"""Инфраструктурный публичный интерфейс слоя базы данных."""
 
 from db_core.model_base import Base
 
-from auth_users.models import User
-
-
-# ==============================================================================
-# auth_users.User — ИМПОРТ В КОНЦЕ.
-# Спека просит явный `from auth_users.models import User`. Сделать это "в общем
-# списке" невозможно: циркулярный импорт
-#   auth_users.models -> db_core.model_base -> db_core.__init__ -> auth_users.models
-# ломается на partially-loaded модуле.
-#
-# На практике цепочка загрузки проекта всегда идёт через main.py ->
-# create_fastapi -> db_core -> ..., поэтому db_core полностью загружается ДО
-# того, как что-то потребует auth_users.models. Помещаем импорт в самый конец —
-# когда этот код выполняется, частичной загрузки auth_users.models ещё не было
-# (его триггерит сам импорт), Python грузит модуль, класс User определяется и
-# попадает в Base.metadata.
-# ==============================================================================
+__all__ = ("Base",)
diff --git a/fastapi-application/main.py b/fastapi-application/main.py
index 9122413..f3af0ec 100644
--- a/fastapi-application/main.py
+++ b/fastapi-application/main.py
@@ -1,17 +1,18 @@
-import db_core
-import uvicorn
 from api import router_api
 from auth_users import router as auth_users_router
 from base_dir_path import BASE_DIR
 from config_log import logF
 from core.config import settings
 from create_fastapi import create_app
+from db_core.model_registry import load_model_registry
 from ex_order_product.router_order_one import r_order_one
 from md_articles.setup_frontend import (
     include_router_api_frontend,
     mount_vite_react_assets,
 )
 
+load_model_registry()
+
 main_app = create_app(custom_docs_url=False)
 
 main_app.include_router(router_api)
diff --git a/fastapi-application/md_articles/__init__.py b/fastapi-application/md_articles/__init__.py
index 204ceb2..a5550e3 100644
--- a/fastapi-application/md_articles/__init__.py
+++ b/fastapi-application/md_articles/__init__.py
@@ -11,7 +11,3 @@
 JWT-in-cookie через fastapi-users. art-роуты используют `Depends(active_user)`
 из auth_users; SessionMiddleware, самописный CSRF и helpers удалены.
 """
-
-from md_articles.setup_frontend import include_router_api_frontend, mount_vite_react_assets
-
-__all__ = ["include_router_api_frontend", "mount_vite_react_assets"]
